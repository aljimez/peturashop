# Example env for local testing with Stripe Checkout
# ----------------------------
# WARNING: This file contains the STRIPE_SECRET_KEY which MUST remain private
# and never be committed to version control. In production store this in a
# secure secrets manager (AWS Secrets Manager, Vault, etc.).

# Stripe secret key (test) - server only
STRIPE_SECRET_KEY=sk_test_xxx

# Optional server port
SERVER_PORT=4242

# When deploying: set STRIPE_SECRET_KEY to your live key and configure a
# webhook signing secret if you add webhook handlers.
